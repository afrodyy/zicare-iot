// import { json } from '@sveltejs/kit';
// import { getGeo } from '$lib/utils/geo';

// export async function GET({ url }) {
// 	const level = url.searchParams.get('level') ?? '0';
// 	const id = url.searchParams.get('id') ?? null;
// 	let data = await getGeo(level, id);
// 	return json(data);
// }
